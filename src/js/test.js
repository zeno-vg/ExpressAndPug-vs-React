import React from "react";
import { renderToString } from 'react-dom/server';
//var React = require("react");	

export defautl class Article extends React.Component{
	render(){
		return[
			{props}
		];
	}
	renderAsString(){
		return renderToString(render());
	}
}